# i18n EN/ES Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add full English/Spanish bilingual support with browser auto-detection and navbar toggle.

**Architecture:** Custom JSON dictionaries loaded server-side, provided to client components via React Context. Locale detection via `proxy.js` (Next.js 16 convention). URLs use `/[lang]/` prefix.

**Tech Stack:** Next.js 16, React Context, JSON dictionaries.

---

### Task 1: Create dictionary JSON files

**Files:**
- Create: `dictionaries/en.json`
- Create: `dictionaries/es.json`

- [ ] **Create `dictionaries/en.json`**

```json
{
  "meta": {
    "title": "Lumen X Labs - Digital Solutions",
    "description": "Modernize, automate, and scale your business with AI-powered solutions."
  },
  "nav": {
    "home": "Home",
    "about": "About",
    "projects": "Projects",
    "contact": "Contact"
  },
  "hero": {
    "title": "We Modernize. We Automate. We Scale.",
    "description": "From fragmented chaos to streamlined precision — we design systems, not just websites.",
    "cta_primary": "View Our Work",
    "cta_secondary": "Get in Touch",
    "badge": "Interactive Lab"
  },
  "pain": {
    "heading": "The Cost of Inefficiency",
    "subtitle": "Most businesses operate in a state of invisible chaos. Slow processes, human error, and the feeling that you're the only one holding it all together.",
    "items": [
      {
        "title": "Fragmented Processes",
        "desc": "Information scattered across spreadsheets and emails, creating bottlenecks."
      },
      {
        "title": "High Operational Load",
        "desc": "Repetitive tasks consume your team's time, limiting growth capacity."
      },
      {
        "title": "Outdated Systems",
        "desc": "Tools that don't communicate, forcing manual data entry."
      }
    ]
  },
  "method": {
    "heading": "My Transformation Methodology",
    "steps": [
      {
        "title": "Audit",
        "desc": "I deeply analyze your current operations, identifying bottlenecks and time leaks that go unnoticed."
      },
      {
        "title": "Design",
        "desc": "I create a new optimized process map, removing friction and designing the shortest path to results."
      },
      {
        "title": "Connect",
        "desc": "I implement the technology (AI, Web, Automations) so the process runs itself with surgical precision."
      }
    ]
  },
  "capacities": {
    "heading": "Our Key Capabilities",
    "items": [
      { "title": "Process Automation", "desc": "Orchestration across systems, execution without human intervention." },
      { "title": "Integrated AI", "desc": "AI models for real-time decision making and operations." },
      { "title": "Data Integration", "desc": "Connecting sources and normalizing for a single source of truth." },
      { "title": "Governance & Compliance", "desc": "Policies, security, and data quality across your stack." },
      { "title": "User Experience", "desc": "Refined UI/UX, accessibility, and performance." },
      { "title": "Business Strategy", "desc": "Digital transformation aligned with goals and ROI." }
    ]
  },
  "proof": {
    "heading": "Proof & Credibility",
    "items": [
      { "name": "Success Case A", "text": "Reduced delivery times by 40% in 90 days." },
      { "name": "Success Case B", "text": "Increased process efficiency by 35%." },
      { "name": "Success Case C", "text": "Estimated ROI of 3x in 12 months." }
    ]
  },
  "showcase": {
    "heading": "Case Studies",
    "items": [
      { "title": "Project Case 1", "desc": "Brief description of the case study." },
      { "title": "Project Case 2", "desc": "Brief description of the case study." },
      { "title": "Project Case 3", "desc": "Brief description of the case study." },
      { "title": "Project Case 4", "desc": "Brief description of the case study." },
      { "title": "Project Case 5", "desc": "Brief description of the case study." },
      { "title": "Project Case 6", "desc": "Brief description of the case study." }
    ]
  },
  "call": {
    "heading": "Ready to Get Started?",
    "subtitle": "Schedule a 30-minute consultation to understand your goals and map a transformation roadmap.",
    "form": {
      "name_placeholder": "Name",
      "email_placeholder": "Email",
      "phone_placeholder": "Phone",
      "company_placeholder": "Company",
      "submit": "Request Consultation",
      "alert": "Thank you for your interest. We'll be in touch soon."
    }
  },
  "footer": {
    "about_title": "About",
    "about_text": "Lumen X Labs is a digital solutions company specializing in business modernization, automation, and AI integrations.",
    "quick_links": "Quick Links",
    "services": "Services",
    "service_items": ["Video Production", "Analytics", "Interactive Design", "Web Development"],
    "connect": "Connect",
    "connect_text": "Get in touch with us for inquiries and collaborations.",
    "copyright": "All rights reserved.",
    "privacy": "Privacy",
    "terms": "Terms",
    "cookies": "Cookies"
  }
}
```

- [ ] **Create `dictionaries/es.json`**

```json
{
  "meta": {
    "title": "Lumen X Labs - Soluciones Digitales",
    "description": "Moderniza, automatiza y escala tu negocio con soluciones impulsadas por IA."
  },
  "nav": {
    "home": "Inicio",
    "about": "Nosotros",
    "projects": "Proyectos",
    "contact": "Contacto"
  },
  "hero": {
    "title": "Modernizamos. Automatizamos. Escalamos.",
    "description": "Del caos fragmentado a la precisión optimizada — diseñamos sistemas, no solo sitios web.",
    "cta_primary": "Ver Nuestro Trabajo",
    "cta_secondary": "Contáctanos",
    "badge": "Laboratorio Interactivo"
  },
  "pain": {
    "heading": "El Costo de la Ineficiencia",
    "subtitle": "Muchos negocios operan en un estado de caos invisible. Procesos lentos, errores humanos y la sensación de que el dueño es la única pieza que mantiene todo unido.",
    "items": [
      {
        "title": "Procesos Fragmentados",
        "desc": "Información dispersa en múltiples hojas de cálculo y correos, generando cuellos de botella."
      },
      {
        "title": "Carga Operativa Alta",
        "desc": "Tareas repetitivas que consumen el tiempo de tu equipo, limitando la capacidad de crecimiento."
      },
      {
        "title": "Sistemas Obsoletos",
        "desc": "Herramientas que no se comunican entre sí, obligando a la entrada manual de datos."
      }
    ]
  },
  "method": {
    "heading": "Mi Metodología de Transformación",
    "steps": [
      {
        "title": "Reviso",
        "desc": "Analizo a fondo tu operación actual, identificando cuellos de botella y fugas de tiempo que pasan desapercibidas."
      },
      {
        "title": "Diseño",
        "desc": "Creo un nuevo mapa de procesos optimizado, eliminando la fricción y diseñando la ruta más corta hacia el resultado."
      },
      {
        "title": "Conecto",
        "desc": "Implemento la tecnología (IA, Web, Automatizaciones) para que el proceso se ejecute solo, con precisión quirúrgica."
      }
    ]
  },
  "capacities": {
    "heading": "Nuestras Capacidades Clave",
    "items": [
      { "title": "Automatización de Procesos", "desc": "Orquestación entre sistemas, ejecución sin intervención humana." },
      { "title": "IA Integrada", "desc": "Modelos IA para toma de decisiones y operaciones en tiempo real." },
      { "title": "Integración de Datos", "desc": "Conexión de fuentes y normalización para una visión única." },
      { "title": "Gobernanza y Cumplimiento", "desc": "Políticas, seguridad y calidad de datos en tu stack." },
      { "title": "Experiencia de Usuario", "desc": "UI/UX refinada, accesibilidad y rendimiento." },
      { "title": "Estrategia de Negocio", "desc": "Transformación digital alineada con objetivos y ROI." }
    ]
  },
  "proof": {
    "heading": "Pruebas y Credibilidad",
    "items": [
      { "name": "Caso de éxito A", "text": "Reduje tiempos de entrega en un 40% en 90 días." },
      { "name": "Caso de éxito B", "text": "Aumenté la eficiencia de procesos en un 35%." },
      { "name": "Caso de éxito C", "text": "ROI estimado de 3x a 12 meses." }
    ]
  },
  "showcase": {
    "heading": "Casos de Estudio",
    "items": [
      { "title": "Caso de proyecto 1", "desc": "Breve descripción del caso de estudio." },
      { "title": "Caso de proyecto 2", "desc": "Breve descripción del caso de estudio." },
      { "title": "Caso de proyecto 3", "desc": "Breve descripción del caso de estudio." },
      { "title": "Caso de proyecto 4", "desc": "Breve descripción del caso de estudio." },
      { "title": "Caso de proyecto 5", "desc": "Breve descripción del caso de estudio." },
      { "title": "Caso de proyecto 6", "desc": "Breve descripción del caso de estudio." }
    ]
  },
  "call": {
    "heading": "¿Listo para empezar?",
    "subtitle": "Agenda una consultoría de 30 minutos para entender tus objetivos y mapear una ruta de transformación.",
    "form": {
      "name_placeholder": "Nombre",
      "email_placeholder": "Email",
      "phone_placeholder": "Teléfono",
      "company_placeholder": "Empresa",
      "submit": "Solicitar Consulta",
      "alert": "Gracias por tu interés. Nos pondremos en contacto pronto."
    }
  },
  "footer": {
    "about_title": "Acerca de",
    "about_text": "Lumen X Labs es una empresa de soluciones digitales especializada en modernización empresarial, automatización e integraciones de IA.",
    "quick_links": "Enlaces Rápidos",
    "services": "Servicios",
    "service_items": ["Producción de Video", "Analítica", "Diseño Interactivo", "Desarrollo Web"],
    "connect": "Conectar",
    "connect_text": "Contáctanos para consultas y colaboraciones.",
    "copyright": "Todos los derechos reservados.",
    "privacy": "Privacidad",
    "terms": "Términos",
    "cookies": "Cookies"
  }
}
```

---

### Task 2: Create get-dictionary.ts (server-side loader)

**Files:**
- Create: `src/lib/get-dictionary.ts`

- [ ] **Create `src/lib/get-dictionary.ts`**

```ts
import 'server-only';

const dictionaries = {
  en: () => import('../../dictionaries/en.json').then((m) => m.default),
  es: () => import('../../dictionaries/es.json').then((m) => m.default),
};

export type Locale = keyof typeof dictionaries;

export const locales = Object.keys(dictionaries) as Locale[];

export const hasLocale = (locale: string): locale is Locale =>
  locale in dictionaries;

export const getDictionary = async (locale: Locale) => dictionaries[locale]();
```

---

### Task 3: Create locale-context.tsx (React Context + provider)

**Files:**
- Create: `src/lib/locale-context.tsx`

- [ ] **Create `src/lib/locale-context.tsx`**

```ts
'use client';

import React, { createContext, useContext, ReactNode } from 'react';

type Dict = Record<string, any>;

interface LocaleContextValue {
  locale: string;
  t: (key: string) => string;
}

const LocaleContext = createContext<LocaleContextValue | null>(null);

function resolve(obj: Dict, path: string): string {
  const keys = path.split('.');
  let current: any = obj;
  for (const key of keys) {
    if (current == null || typeof current !== 'object') return path;
    current = current[key];
  }
  return typeof current === 'string' ? current : path;
}

export function LocaleProvider({
  locale,
  dictionary,
  children,
}: {
  locale: string;
  dictionary: Dict;
  children: ReactNode;
}) {
  const value: LocaleContextValue = {
    locale,
    t: (key: string) => resolve(dictionary, key),
  };
  return (
    <LocaleContext.Provider value={value}>{children}</LocaleContext.Provider>
  );
}

export function useLocale(): LocaleContextValue {
  const ctx = useContext(LocaleContext);
  if (!ctx) {
    // Fallback for components rendered outside the provider
    return { locale: 'en', t: (key: string) => key };
  }
  return ctx;
}
```

---

### Task 4: Create proxy.js (locale detection + redirect)

**Files:**
- Create: `proxy.js` (root level)

- [ ] **Create `proxy.js`**

```js
import { NextResponse } from 'next/server';

const locales = ['en', 'es'];
const defaultLocale = 'en';

function getLocale(request) {
  const acceptLang = request.headers.get('accept-language');
  if (!acceptLang) return defaultLocale;

  // Match browser language against supported locales
  for (const locale of locales) {
    if (acceptLang.startsWith(locale) || acceptLang.includes(` ${locale}`)) {
      return locale;
    }
  }
  return defaultLocale;
}

export function proxy(request) {
  const { pathname } = request.nextUrl;

  // Check if pathname already has a locale
  const pathnameHasLocale = locales.some(
    (locale) => pathname.startsWith(`/${locale}/`) || pathname === `/${locale}`
  );

  if (pathnameHasLocale) return;

  // Redirect to locale-prefixed path
  const locale = getLocale(request);
  request.nextUrl.pathname = `/${locale}${pathname}`;
  return NextResponse.redirect(request.nextUrl);
}

export const config = {
  matcher: ['/((?!_next|favicon|videos|images|fonts).*)'],
};
```

---

### Task 5: Restructure layout - root + [lang]

**Files:**
- Modify: `src/app/layout.tsx` — simplify to just html + registry
- Create: `src/app/[lang]/layout.tsx` — locale-aware layout with all providers

- [ ] **Replace `src/app/layout.tsx`**

```tsx
import type { Metadata } from "next";
import { Syne, Plus_Jakarta_Sans } from "next/font/google";
import "./globals.css";
import StyledComponentsRegistry from "@/lib/registry";

const syne = Syne({
  variable: "--font-syne",
  subsets: ["latin"],
  weight: ["700", "800"],
});

const jakarta = Plus_Jakarta_Sans({
  variable: "--font-jakarta",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Lumen X Labs - Digital Solutions",
  description: "Creating innovative digital experiences.",
  icons: { icon: '/favicon.ico' },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html className={`${syne.variable} ${jakarta.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">
        <StyledComponentsRegistry>
          {children}
        </StyledComponentsRegistry>
      </body>
    </html>
  );
}
```

- [ ] **Create `src/app/[lang]/layout.tsx`**

```tsx
import { notFound } from 'next/navigation';
import { getDictionary, hasLocale, locales } from '@/lib/get-dictionary';
import { LocaleProvider } from '@/lib/locale-context';
import ThemeProvider from '@/lib/theme-context';
import Header from '@/components/common/Header';
import SmoothScroll from '@/components/common/SmoothScroll';

export async function generateStaticParams() {
  return locales.map((lang) => ({ lang }));
}

export default async function LangLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: Promise<{ lang: string }>;
}) {
  const { lang } = await params;

  if (!hasLocale(lang)) notFound();

  const dictionary = await getDictionary(lang);

  return (
    <LocaleProvider locale={lang} dictionary={dictionary}>
      <ThemeProvider>
        <SmoothScroll>
          <Header />
          {children}
        </SmoothScroll>
      </ThemeProvider>
    </LocaleProvider>
  );
}
```

---

### Task 6: Move page.tsx to [lang]/page.tsx

**Files:**
- Move: `src/app/page.tsx` → `src/app/[lang]/page.tsx`

- [ ] **Move and update `src/app/[lang]/page.tsx`**

```tsx
'use client';
import { useLocale } from '@/lib/locale-context';
import Hero from '@/components/sections/Hero';
import PainSection from '@/components/sections/PainSection';
import MethodSection from '@/components/sections/MethodSection';
import CapacitiesSection from '@/components/sections/CapacitiesSection';
import ProofSection from '@/components/sections/ProofSection';
import ShowcaseSection from '@/components/sections/ShowcaseSection';
import CallSection from '@/components/sections/CallSection';
import Footer from '@/components/common/Footer';

export default function Home() {
  const { t } = useLocale();

  return (
    <>
      <Hero />
      <PainSection />
      <MethodSection />
      <CapacitiesSection />
      <ProofSection />
      <ShowcaseSection />
      <CallSection />
      <Footer />
    </>
  );
}
```

Note: Hero no longer receives props — it reads translations internally.

---

### Task 7: Update Header.tsx — add locale toggle + use t()

**Files:**
- Modify: `src/components/common/Header.tsx`

- [ ] **Update `Header.tsx` — import useLocale, add globe toggle, translate nav links**

Changes:
1. Import `useLocale` and `usePathname`/`useRouter`
2. Replace hardcoded nav link text with `{t('nav.home')}`, etc.
3. Add globe icon button next to theme toggle that switches locale

Add these imports:
```ts
import { useLocale } from '@/lib/locale-context';
import { usePathname, useRouter } from 'next/navigation';
```

Inside component, add:
```ts
const { t, locale } = useLocale();
const pathname = usePathname();
const router = useRouter();
```

Replace nav text:
```tsx
<LinkItem href="/" $active>{t('nav.home')}</LinkItem>
<LinkItem href="/about">{t('nav.about')}</LinkItem>
<LinkItem href="/projects">{t('nav.projects')}</LinkItem>
<CTA href="/contact">{t('nav.contact')}</CTA>
```

Add globe toggle:
```tsx
<button
  onClick={() => {
    const newLocale = locale === 'en' ? 'es' : 'en';
    const newPath = pathname.replace(/^\/[a-z]{2}/, `/${newLocale}`);
    router.push(newPath);
  }}
  style={{
    background: 'var(--surface-secondary)',
    border: '1px solid var(--border)',
    color: 'var(--text-primary)',
    width: 40,
    height: 40,
    borderRadius: '50%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    cursor: 'pointer',
    fontSize: 16,
    marginLeft: 8,
  }}
  title="Toggle language"
>
  {locale === 'en' ? 'ES' : 'EN'}
</button>
```

---

### Task 8: Update Hero.tsx — use locale internally

**Files:**
- Modify: `src/components/sections/Hero.tsx`

- [ ] **Update `Hero.tsx` — use `useLocale()` instead of props**

Remove `HeroProps` interface and use locale internally:

```tsx
import { useLocale } from '@/lib/locale-context';

export default function Hero() {
  const { t } = useLocale();

  return (
    <HeroSection>
      <HeroContent>
        <div className="flex flex-col">
          <TitleWrapper>
            <Title>{t('hero.title')}</Title>
          </TitleWrapper>
          <DescriptionWrapper>
            <Description>{t('hero.description')}</Description>
          </DescriptionWrapper>
          <ButtonGroup>
            <Button>{t('hero.cta_primary')}</Button>
            <Button $variant="secondary">{t('hero.cta_secondary')}</Button>
          </ButtonGroup>
        </div>
        <VisualElement>
          <div className="relative z-10 p-12 text-center">
            <div className="text-6xl mb-4">⚡</div>
            <div className="font-syne font-bold text-sm uppercase tracking-widest opacity-60">
              {t('hero.badge')}
            </div>
          </div>
        </VisualElement>
      </HeroContent>
    </HeroSection>
  );
}
```

Remove `HeroProps`, `videoSrc`, `VideoHeroDynamic`, and the `interface HeroProps`.

---

### Task 9: Update PainSection.tsx — use t() for all text

**Files:**
- Modify: `src/components/sections/PainSection.tsx`

- [ ] **Replace hardcoded data with t()**

```tsx
'use client';
import styled from 'styled-components';
import { motion } from 'framer-motion';
import { useLocale } from '@/lib/locale-context';

// ...styles stay the same...

export default function PainSection() {
  const { t } = useLocale();
  
  const painPoints = [
    { title: t('pain.items.0.title'), desc: t('pain.items.0.desc') },
    { title: t('pain.items.1.title'), desc: t('pain.items.1.desc') },
    { title: t('pain.items.2.title'), desc: t('pain.items.2.desc') },
  ];

  return (
    <Section>
      <ContentWrapper>
        <Heading>{t('pain.heading')}</Heading>
        <Text>{t('pain.subtitle')}</Text>
      </ContentWrapper>
      <ChaosGrid>
        {painPoints.map((point, i) => (
          <ChaosCard key={i}
            initial={{ opacity: 0, scale: 0.9 }}
            whileInView={{ opacity: 1, scale: 1 }}
            viewport={{ once: true }}
            transition={{ duration: 0.5, delay: i * 0.2 }}
          >
            <h3>{point.title}</h3>
            <p>{point.desc}</p>
          </ChaosCard>
        ))}
      </ChaosGrid>
    </Section>
  );
}
```

---

### Task 10: Update MethodSection.tsx — use t()

**Files:**
- Modify: `src/components/sections/MethodSection.tsx`

- [ ] **Replace hardcoded data with t()**

```tsx
import { useLocale } from '@/lib/locale-context';

export default function MethodSection() {
  const { t } = useLocale();
  
  const steps = [
    { title: t('method.steps.0.title'), desc: t('method.steps.0.desc') },
    { title: t('method.steps.1.title'), desc: t('method.steps.1.desc') },
    { title: t('method.steps.2.title'), desc: t('method.steps.2.desc') },
  ];

  return (
    <Section>
      <Heading>{t('method.heading')}</Heading>
      <MethodGrid>
        {steps.map((step, i) => (
          <MethodStep key={i} ...>
            <div className="step-number">0{i + 1}</div>
            <h3>{step.title}</h3>
            <p>{step.desc}</p>
          </MethodStep>
        ))}
      </MethodGrid>
    </Section>
  );
}
```

---

### Task 11: Update CapacitiesSection.tsx — use t()

**Files:**
- Modify: `src/components/sections/CapacitiesSection.tsx`

- [ ] **Replace hardcoded data with t()**

```tsx
import { useLocale } from '@/lib/locale-context';

export default function CapacitiesSection() {
  const { t } = useLocale();
  
  const capabilities = [
    { title: t('capacities.items.0.title'), desc: t('capacities.items.0.desc') },
    { title: t('capacities.items.1.title'), desc: t('capacities.items.1.desc') },
    { title: t('capacities.items.2.title'), desc: t('capacities.items.2.desc') },
    { title: t('capacities.items.3.title'), desc: t('capacities.items.3.desc') },
    { title: t('capacities.items.4.title'), desc: t('capacities.items.4.desc') },
    { title: t('capacities.items.5.title'), desc: t('capacities.items.5.desc') },
  ];

  return (
    <Section id="capacities">
      <Heading>{t('capacities.heading')}</Heading>
      <Grid>
        {capabilities.map((c, idx) => (
          <Card key={idx} whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }}>
            <CardTitle>{c.title}</CardTitle>
            <CardDesc>{c.desc}</CardDesc>
          </Card>
        ))}
      </Grid>
    </Section>
  );
}
```

---

### Task 12: Update ProofSection.tsx — use t()

**Files:**
- Modify: `src/components/sections/ProofSection.tsx`

- [ ] **Replace hardcoded data with t()**

Same pattern: import `useLocale`, replace heading and items with `t('proof.items.N.name')`, `t('proof.items.N.text')`.

---

### Task 13: Update ShowcaseSection.tsx — use t()

**Files:**
- Modify: `src/components/sections/ShowcaseSection.tsx`

- [ ] **Replace hardcoded data with t()**

Import `useLocale`, replace heading and map `showcase.items`.

---

### Task 14: Update CallSection.tsx — use t() for all text

**Files:**
- Modify: `src/components/sections/CallSection.tsx`

- [ ] **Replace hardcoded text with t()**

```tsx
import { useLocale } from '@/lib/locale-context';

export default function CallSection() {
  const { t } = useLocale();
  const onSubmit = (e: React.FormEvent) => { e.preventDefault(); alert(t('call.form.alert')); };

  return (
    <Section id="contact">
      <Heading>{t('call.heading')}</Heading>
      <Sub>{t('call.subtitle')}</Sub>
      <Form onSubmit={onSubmit}>
        <Input placeholder={t('call.form.name_placeholder')} required />
        <Input placeholder={t('call.form.email_placeholder')} type="email" required />
        <Input placeholder={t('call.form.phone_placeholder')} />
        <Input placeholder={t('call.form.company_placeholder')} />
        <div style={{gridColumn:'1 / -1'}}>
          <Button type="submit">{t('call.form.submit')}</Button>
        </div>
      </Form>
    </Section>
  );
}
```

---

### Task 15: Update Footer.tsx — use t()

**Files:**
- Modify: `src/components/common/Footer.tsx`

- [ ] **Replace hardcoded text with t()**

Import `useLocale`, replace all text: section titles, link names, services, connect text, copyright.

---

### Task 16: Final verification

**Files:**
- Run: lint check

- [ ] **Run lint**
```bash
npm run lint
```
Expected: no errors, no warnings.

- [ ] **Verify dev server**
```bash
npm run dev
```
- Visit `/` — should auto-redirect to `/en` or `/es` based on browser language
- Visit `/en` — should show English
- Visit `/es` — should show Spanish
- Click globe toggle — should switch language
